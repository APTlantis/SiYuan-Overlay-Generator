# Changelog

## Unreleased

- Established the Rust CLI package and normalized project-model boundary.
- Added deterministic, read-only discovery with fixture immutability coverage.
- Added Markdown ZIP projection with overview, indexes, directory pages, code
  wrappers, artifact records, provenance, and structure visualization.
- Validated Markdown ZIP import with SiYuan v3.8.3.
- Adjusted package paths for SiYuan import behavior: no redundant ZIP root and
  package-only escaping of an `assets` directory component.

This is not a release record. See the manifest and success criteria for current
scope and completion evidence.
